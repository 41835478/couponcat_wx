<!-- 优惠券列表 -->
<scroll-view scroll-y="true">
  <view wx:for="{{coupons}}">
    <view wx:for="{{item.lists}}">
      <image src="{{item.img}}"></image>
      <text>{{item.title}}</text>
      <text>{{item.price}}</text>
      <text>{{item.time}}</text>
    </view>
  </view>
</scroll-view>